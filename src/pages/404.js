import React from 'react'
import SelfIcon from '../assets/images/favicon.ico'

const NotFoundPage = () => (
  <div>
    <img src={SelfIcon} alt="rashmi ap" />
    <p>You just hit a route that doesn&#39;t exist... the sadness.</p>
  </div>
)

export default NotFoundPage
